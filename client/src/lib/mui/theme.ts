import { createTheme } from "@mui/material";

const theme = createTheme({
  palette: {
    background: {
      default: "#222",
    },
    primary: {
      main: "#f00",
    },
  },
});

export default theme;
