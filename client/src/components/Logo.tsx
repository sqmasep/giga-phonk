import { Typography } from "@mui/material";
import React from "react";

interface LogoProps {}

const Logo: React.FC<LogoProps> = ({}) => {
  return <Typography>giga-phonk</Typography>;
};

export default Logo;
